<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.lawconsultserver.mapper.AuditLogMapper">

    <resultMap id="AuditLogResult" type="com.example.lawconsultserver.entity.AuditLog">
        <id column="id" property="id"/>
        <result column="admin_id" property="adminId"/>
        <result column="target_type" property="targetType"/>
        <result column="target_id" property="targetId"/>
        <result column="action" property="action"/>
        <result column="reason" property="reason"/>
        <result column="old_status" property="oldStatus"/>
        <result column="new_status" property="newStatus"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <!-- 记录操作日志 -->
    <insert id="insertLog" parameterType="com.example.lawconsultserver.entity.AuditLog" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO audit_logs (admin_id, target_type, target_id, action, reason, old_status, new_status, created_at)
        VALUES (#{adminId}, #{targetType}, #{targetId}, #{action}, #{reason}, #{oldStatus}, #{newStatus}, NOW())
    </insert>

    <!-- 查询最近操作日志 -->
    <select id="selectRecentLogs" resultMap="AuditLogResult">
        SELECT * FROM audit_logs
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>

    <!-- 分页查询操作日志 -->
    <select id="selectLogs" resultMap="AuditLogResult">
        SELECT * FROM audit_logs
        ORDER BY created_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <!-- 统计操作日志总数 -->
    <select id="countLogs" resultType="long">
        SELECT COUNT(*) FROM audit_logs
    </select>

</mapper>
