<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.lawconsultserver.mapper.CarouselItemMapper">

    <resultMap id="CarouselItemResult" type="com.example.lawconsultserver.entity.CarouselItem">
        <id column="id" property="id"/>
        <result column="admin_id" property="adminId"/>
        <result column="title" property="title"/>
        <result column="summary" property="summary"/>
        <result column="content" property="content"/>
        <result column="image_url" property="imageUrl"/>
        <result column="category" property="category"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="status" property="status"/>
        <result column="start_time" property="startTime"/>
        <result column="end_time" property="endTime"/>
        <result column="view_count" property="viewCount"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insertCarouselItem" parameterType="com.example.lawconsultserver.entity.CarouselItem" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO carousel_items (
            admin_id, title, summary, content, image_url, category, sort_order, status, start_time, end_time, created_at, updated_at
        )
        VALUES (
            #{adminId}, #{title}, #{summary}, #{content}, #{imageUrl}, #{category}, #{sortOrder}, #{status}, #{startTime}, #{endTime}, NOW(), NOW()
        )
    </insert>

    <select id="selectPublicActive" resultMap="CarouselItemResult">
        SELECT *
        FROM carousel_items
        WHERE status = 'active'
          AND (start_time IS NULL OR start_time &lt;= NOW())
          AND (end_time IS NULL OR end_time &gt;= NOW())
        ORDER BY sort_order ASC, updated_at DESC
    </select>

    <select id="selectAll" resultMap="CarouselItemResult">
        SELECT *
        FROM carousel_items
        <where>
            <if test="keyword != null and keyword != ''">
                AND (title LIKE CONCAT('%', #{keyword}, '%')
                     OR category LIKE CONCAT('%', #{keyword}, '%')
                     OR summary LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY sort_order ASC, updated_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*)
        FROM carousel_items
        <where>
            <if test="keyword != null and keyword != ''">
                AND (title LIKE CONCAT('%', #{keyword}, '%')
                     OR category LIKE CONCAT('%', #{keyword}, '%')
                     OR summary LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
    </select>

    <select id="selectById" resultMap="CarouselItemResult">
        SELECT * FROM carousel_items WHERE id = #{id}
    </select>

    <update id="updateCarouselItem" parameterType="com.example.lawconsultserver.entity.CarouselItem">
        UPDATE carousel_items
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="summary != null">summary = #{summary},</if>
            <if test="content != null">content = #{content},</if>
            <if test="imageUrl != null">image_url = #{imageUrl},</if>
            <if test="category != null">category = #{category},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
            <if test="status != null">status = #{status},</if>
            start_time = #{startTime},
            end_time = #{endTime},
            updated_at = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM carousel_items WHERE id = #{id}
    </delete>

</mapper>
