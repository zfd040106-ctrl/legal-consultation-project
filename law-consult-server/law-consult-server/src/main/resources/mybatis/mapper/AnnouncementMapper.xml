<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.lawconsultserver.mapper.AnnouncementMapper">

    <resultMap id="AnnouncementResult" type="com.example.lawconsultserver.entity.Announcement">
        <id column="id" property="id"/>
        <result column="admin_id" property="adminId"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="is_pinned" property="isPinned"/>
        <result column="status" property="status"/>
        <result column="published_at" property="publishedAt"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insertAnnouncement" parameterType="com.example.lawconsultserver.entity.Announcement" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO announcements (admin_id, title, content, is_pinned, status, published_at, created_at, updated_at)
        VALUES (#{adminId}, #{title}, #{content}, #{isPinned}, #{status}, NOW(), NOW(), NOW())
    </insert>

    <select id="selectAll" resultMap="AnnouncementResult">
        SELECT * FROM announcements
        <where>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="keyword != null and keyword != ''">
                AND title LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
        ORDER BY is_pinned DESC, published_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM announcements
        <where>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="keyword != null and keyword != ''">
                AND title LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
    </select>

    <update id="updateAnnouncement" parameterType="com.example.lawconsultserver.entity.Announcement">
        UPDATE announcements
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="content != null">content = #{content},</if>
            <if test="isPinned != null">is_pinned = #{isPinned},</if>
            <if test="status != null">status = #{status},</if>
            updated_at = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteAnnouncement">
        DELETE FROM announcements WHERE id = #{id}
    </delete>

    <delete id="deleteById">
        DELETE FROM announcements WHERE id = #{id}
    </delete>

    <update id="updatePin">
        UPDATE announcements
        SET is_pinned = #{isPinned},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <select id="countPinned" resultType="long">
        SELECT COUNT(*) FROM announcements
        WHERE is_pinned = TRUE
    </select>

</mapper>
